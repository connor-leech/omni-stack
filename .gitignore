# Secrets — NEVER commit these
keys.txt
monochrome-formatter.txt
.env
*.local.*

# Rendered configs with substituted keys (contain real API keys)
configs/rendered/
# Legacy / loose import files (kept ignored for safety if anyone renames a rendered file)
configs/*.rendered.json
configs/aiostreams.json
configs/aiometadata.json

# Runtime data
data/

# Working/reference material (prompt doc + screenshots, ~6.5MB of JPEGs)
claude/

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
